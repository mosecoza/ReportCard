<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:compat="http://schemas.android.com/tools"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_register"
        android:title="@string/register_new_student"
        android:titleCondensed="Register"
        android:orderInCategory="1"/>

    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/action_view_all"
        android:title="@string/view_all_students"
        android:titleCondensed="View"
        android:orderInCategory="2"/>
    <item
        android:id="@+id/action_edit"
        android:title="@string/edit_student_data"
        android:titleCondensed="Edit"
        android:orderInCategory="3"/>

</menu>